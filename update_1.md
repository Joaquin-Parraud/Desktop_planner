1. Added a feature so that the tasks can be marked as completed.
2. Added a feature so that tasks can be deleted.
3. Added a feature so that tasks can be changed of grouping by dragging them (kind of like VS code directories)
4. Added a feature so that task groupings in the left hand side are collapsable
5. added a feature so that tasks can have a description
6. Added a feature so that task groupings can have a description
7. Added the option to add hour to the tasks